import { Button } from "@/components/ui/button";
import { cn } from "@/lib/utils";

export default function Home() {
  return (
    <Button className={cn("border-blue-500 bg-blue-500 text-white")}>
      BEWEAR
    </Button>
  );
}
